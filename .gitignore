node_modules/
.claude/settings.local.json
.vercel/
dist/
build/
out/
*.log
_pill*.png
.tools/

# Local ASR models are never committed.
models/*
!models/README.md

# Downloaded whisper.cpp binaries (fetched, not source).
native/asr/*.exe
native/asr/*.dll
!native/asr/README.md

# Local SQLite databases.
*.sqlite
*.sqlite3
*.db

# Android local config — holds SDK path and the build-time Groq key. Never commit.
local.properties
.gradle/
