<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:label="ColdVoice"
        android:supportsRtl="true">

        <!-- The ColdVoice voice keyboard (IME). -->
        <service
            android:name=".ime.ColdVoiceImeService"
            android:label="ColdVoice Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Optional side-bubble overlay. Requires explicit user permission and
             is used ONLY to detect editable inputs and position the bubble. -->
        <service
            android:name=".a11y.ColdVoiceBubbleService"
            android:label="ColdVoice Bubble"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
